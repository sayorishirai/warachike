<header id="header">
	<div class="container">
	  	<div class="row">
	  		<%= link_to(root_path) do %>
	  			<div class="col-md-4 col-md-offset-4"><%= image_tag("logo3.gif") %></div>
	  		<% end %>
		    <% if user_signed_in? %>
		    	<div class="pull-right">
		    		<%= link_to(user_path(current_user)) do %>
		    			<div class="mypage-menu"><i class="far fa-smile fa-lg faa-ring animated"></i>マイページ</div>
		    		<% end %>
		    	</div>
		    	<% else %>
			    <!-- 何も表示しない -->
			<% end %>
	  	</div><!-- /row -->
	</div><!-- /container -->
</header><!-- /header -->

